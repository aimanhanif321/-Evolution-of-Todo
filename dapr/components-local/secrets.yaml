# Local Secrets Store for Development
# Uses environment variables instead of Kubernetes secrets
apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: taskora-secrets
spec:
  type: secretstores.local.env
  version: v1
  metadata: []
